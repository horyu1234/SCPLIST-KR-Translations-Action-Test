name: Check File Encoding

on: [ push, pull_request ]

jobs:
  file-encoding:
    runs-on: ubuntu-latest

    steps:
      - name: "[Git] Checkout repository"
        uses: actions/checkout@v2

      - name: "[-] Check Encoding is UTF8-NO-BOM"
        run: echo '! find $GITHUB_WORKSPACE -type f -exec file --mime {} \; | grep -v "charset=binary$" | grep -v "charset=utf-8$"' | bash
